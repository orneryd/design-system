/**
 * @jest-environment jest-environment-happy-dom
 */
import './ui-button'

describe('UIChip', () => {
  let element
  beforeEach(() => {
  })

  afterEach(() => {})

  it('should have a shadow root', () => {
  })

})
