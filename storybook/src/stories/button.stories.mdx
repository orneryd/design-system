import { Preview, Meta, Description } from '@storybook/addon-docs/blocks'
import { withKnobs, select, boolean } from '@storybook/addon-knobs'
import '@ornery/ui-button'
import '@ornery/ui-paper'
import README from '@ornery/ui-button-readme'
const label = 'Variants'
const options = {
  Primary: 'primary',
  PrimaryOutlined: 'primary outlined',
  PrimaryRounded: 'primary rounded',
  PrimaryRoundedOutlined: 'primary rounded outlined',
  Secondary: 'secondary',
  SecondaryOutlined: 'secondary outlined',
  SecondaryRounded: 'secondary rounded',
  SecondaryRoundedOutlined: 'secondary rounded outlined',
  Info: 'info',
  InfoOutlined: 'info outlined',
  InfoRounded: 'info rounded',
  InfoRoundedOutlined: 'info rounded outlined',
  Success: 'success',
  SuccessOutlined: 'success outlined',
  SuccessRounded: 'success rounded',
  SuccessRoundedOutlined: 'success rounded outlined',
  Warning: 'warning',
  WarningOutlined: 'warning outlined',
  WarningRounded: 'warning rounded',
  WarningRoundedOutlined: 'warning rounded outlined',
  Danger: 'danger',
  DangerOutlined: 'danger rounded',
  DangerRounded: 'danger outlined',
  DangerRoundedOutlined: 'danger rounded outlined',
  Outlined: 'outlined',
  RoundedOutlined: 'rounded outlined'
}
const defaultValue = 'primary'
const groupId = 'Buttons'
const styles = {
  textTransform: 'capitalize',
  margin: '1rem'
}

<Meta
  title="Web Components/Buttons"
  decorators={[withKnobs]}
  parameters={{
    component: 'ui-button',
    options: { 
      selectedPanel: 'storybookjs/knobs/panel'
     }
  }}
/>

<Description>
    {README}
</Description>

<Preview withToolbar>
  <Story name="All">
    <>
      <link rel="stylesheet" type="text/css" href="/simone/overrides.css" />
      {Object.values(options).map(variant => {
        return (<div style={{margin: '10px 0'}}>
              <ui-button variant={variant}>{variant}</ui-button>
            </div>)
      })}
    </>
  </Story>
</Preview>

<Story name="Button">
  <ui-button
    style={styles}
    variant={select(label, options, defaultValue, groupId)}
    disabled={boolean('Disabled', false, groupId)}
  >
    {select(label, options, defaultValue, groupId)}
  </ui-button>
</Story>

<Story name="Anchor">
  <ui-button
    style={styles}
    href="https://www.google.com"
    target="_blank"
    variant={select(label, options, 'outlined rounded', groupId)}
    disabled={boolean('Disabled', false, groupId)}
  >
    external link
  </ui-button>
</Story>